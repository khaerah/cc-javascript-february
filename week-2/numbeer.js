function number (num) {
    if (num <= 0) {
        return true;
    } else {
        return false;
    }    
}

let number = prompt('this program checks if the number is less than or equal zero');
num = Number(num)

alert((number(num)));